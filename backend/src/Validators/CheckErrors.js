// Function which checks Errors

function CheckErrors(body) {
  let status = false;
  let errorBody = null;

  //   Check errors and Modify status and error body

  return {
    status: status,
    message: errorBody,
  };
}

module.exports = CheckErrors;
